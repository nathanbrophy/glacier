package assets

// Mascot is the project mascot identifier selected at init time.
const Mascot = "{{.Mascot}}"

// MascotKaomoji is the single-line kaomoji form.
const MascotKaomoji = "{{.MascotKaomoji}}"
